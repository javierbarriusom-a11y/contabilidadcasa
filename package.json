{
  "name": "finanzas-casa-def",
  "version": "0.2.0",
  "private": true,
  "description": "Dashboard financiero personal con pruebas de regresión y copias verificables.",
  "scripts": {
    "test": "node --test tests/*.test.*",
    "baseline:capture": "node tools/capture-baseline.mjs"
  }
}
